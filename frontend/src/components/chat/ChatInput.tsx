import { ArrowUp } from "@phosphor-icons/react";
import { useState } from "react";
import { Button } from "../ui/Button";
export function ChatInput({ onSend, loading }: { onSend: (question: string) => void; loading: boolean }) { const [value, setValue] = useState(""); const submit = (event: React.FormEvent) => { event.preventDefault(); if (value.trim() && !loading) { onSend(value.trim()); setValue(""); } }; return <form onSubmit={submit} className="border-t border-line/80 bg-white/75 px-4 pb-3 pt-4"><div className="mx-auto max-w-3xl rounded-[1.35rem] border border-line bg-white p-1.5 shadow-[0_12px_30px_rgba(46,125,50,.09)] transition focus-within:border-brand focus-within:ring-4 focus-within:ring-green-100"><div className="flex items-center gap-2"><input value={value} onChange={(event) => setValue(event.target.value)} placeholder="Hỏi về quy chế, học vụ hoặc tài liệu..." className="h-10 min-w-0 flex-1 bg-transparent px-3 text-[15px] text-ink outline-none placeholder:text-muted" /><Button type="submit" className="size-10 shrink-0 p-0" aria-label="Gửi câu hỏi" disabled={loading}><ArrowUp size={20} weight="bold" /></Button></div><div className="flex items-center justify-between px-3 pb-1 pt-0.5 text-[10px] text-muted"><span>EduRAG sẽ trích dẫn tài liệu khi có kết quả phù hợp.</span><span className="hidden sm:block">Enter để gửi</span></div></div></form>; }
